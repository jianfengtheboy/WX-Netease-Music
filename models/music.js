import { HTTP } from '../..//utils/http.js'

class Music extends HTTP {
      constructor () {
            super()
      }

      //获取歌曲播放src
      getSrc (songid, success) {
            let params = {
                  url: 'music/url',
                  
            }
      }
}